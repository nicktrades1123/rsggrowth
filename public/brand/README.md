# RSG logo

`rsg-logo.png` is the original transparent PNG supplied by the owner during implementation. It is used unchanged in the header and footer, with intrinsic dimensions and proportional CSS sizing. Do not crop, recolor, stretch, trace, or reconstruct it. The source file is kept byte-for-byte intact.
